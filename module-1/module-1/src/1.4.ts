// Basis Data types

// 1. string
let firstName: string = "Rafsan";

2; // number
let age: number = 30;

3; // boolean
let isEmployed: boolean = true;

// 4. null
let middleName: null = null;

// 5. undefined
let address: undefined = undefined;

// its anyType.its not redemanded to use it.
let d;

let f: number;

// 6.Arrays
let numbers: number[] = [1, 2, 3, 4, 5];
let names: string[] = ["Rafsan", "Rafiq", "Rafi"];

7; // tuple
// you can use tuple to define a fixed length array with different types of data.
let person: [string, number] = ["Rafsan", 30]; // [name, age]
